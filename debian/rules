#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/hlibrary.mk

build/ghcjs:: build-ghc-stamp
	runhaskell debian/Setup.hs

clean::
	rm -rf usr debian/ghcjs.install debian/ghcjs.substvars.tmp
